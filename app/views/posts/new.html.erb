<%= form_for @post do |f| %>

	<%= f.label :title %>
	<%= f.text_field :title %>

	<br>

	<%= f.label :content %>
	<%= f.text_area :content %>

	<br>

	<%= f.label :category %>
	<%= f.select :category, Category.all.collect { |c| [c.name, c.id] } %>


	<%= f.submit 'Create' %>

<% end %>